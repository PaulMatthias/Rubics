#include "classes.h"

void check_for_position(Cube cube, int colour_one, int& side_one, int& pos_one, int colour_two, int& side_two, int& pos_two){

  for(int i=0;i<cube.sides.size();i++){
    for(int j=0;j<cube.sides[i].colours.size();j++)
      if(cube.sides[i].colours[j]==colour_one){
        //get neighbour colour and chekc if it is the right one
      }
    }
  }
  



}
